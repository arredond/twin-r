# Generated from services/tiles/pyproject.toml via uv -- regenerate after
# changing that file's dependencies with:
#   cd /path/to/repo && uv export --quiet --package twiner-tiles --no-dev \
#     --no-editable --no-hashes --no-header --no-annotate --no-emit-project \
#     > services/tiles/src/requirements.txt
# Must live at this exact path (the Lambda's PythonFunction `entry`,
# infra/stacks/twiner_stack.py) -- aws-cdk.aws-lambda-python-alpha's
# bundling looks for a requirements.txt right next to the handler code, not
# up at services/tiles/'s own pyproject.toml (a src-layout package, so the
# two aren't the same directory). --no-hashes: the bundling container
# resolves/builds these inside a Lambda-compatible Linux image regardless
# of the host machine this was generated on, so pinning to this machine's
# own downloaded wheel hashes would fail the build there.
mapbox-vector-tile==2.2.0
numpy==2.5.3
pmtiles==3.8.1
protobuf==6.33.6
pyclipper==1.4.0
shapely==2.1.2
