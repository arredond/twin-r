[project]
name = "twiner-infra"
version = "0.1.0"
description = "twiner AWS CDK app (S3 data/results buckets, scenario Lambda) -- see docs/decisions/0001-compute-and-iac.md"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-aws-lambda-python-alpha==2.269.0a0",
    "aws-cdk-lib>=2.150",
    "constructs>=10.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["stacks"]
